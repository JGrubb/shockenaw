<%= simple_form_for @item do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.input :type, collection: ["member bio", "band bio"] %>
  <%= f.button :submit, class: "button small" %>
<% end %>
